[project]
name = "clawaimail"
version = "0.1.0"
description = "ClawAIMail SDK - Email inbox API for AI agents"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
